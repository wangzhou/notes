基础概念分析方法
===

-v0.1 2026.9.8 Sherlock init

简介：分析技术机制类问题的方法论。核心是让AI和人对问题的理解建立在
基础概念(系统调用、共享内存、中断、socket语义)之上，而不是停留在
类比、组件图和抽象名词层面。适用于机制讲解、代码走读、方案讨论。

## 核心原则

机制类问题的解释最终必须落到基础概念：一次数据传递，要能说清是哪个
进程调了什么系统调用、哪些内存被读写、谁通过什么机制唤醒了谁。类比
只用于建立直觉入口，不能替代机制解释。

## 解释的分层

同一个问题可以在不同层面回答。判断讲解是否到位，看是否下沉到了对方
需要的那一层：

| 层次 | cube vsock通信的例子 |
|------|---------------------|
| 组件层 | CubeProxy按Redis路由表转发到Cubelet |
| 协议层 | virtio-vsock报文按CID+port寻址 |
| 基础概念层 | shim的write(fd) -> muxer的epoll_wait醒来 -> read(fd) -> memcpy进guest内存 -> irqfd注入中断 -> agent的read(fd)返回 |

停留在组件层时，人得到的是名词而不是机制，无法在心里模拟数据流。

## 分析方法

1. 从代码出发找实际实现。用grep定位关键函数并读真实代码，不凭名字
   和印象猜测。结论要能给出file:line或函数名。
2. 逐跳拆解链路。把完整路径拆成一段段，对每一跳问同一个问题：
   这一跳实际发生的是什么操作？
3. 用基础原语描述。每次数据传递说清：谁调了什么syscall、缓冲区和
   内存发生了什么、通过什么机制通知对方。
4. 类比只做入口。类比(比如"相当于串口")负责让对方快速定位心智模型，
   之后必须跟进机制细节，不能停在类比上。
5. 对方说"还是不懂"时，下沉一层，而不是在同一层换个说法重复。重复
   抽象解释不产生新信息，下沉到syscall/内存/中断才会。

## 基础概念清单

描述机制时优先使用以下词汇表，它们可验证、无歧义：

用户态: socket/bind/listen/accept/connect/read/write/epoll_wait等
        系统调用; fd和内核缓冲区
内核态: 协议栈分层; 驱动的中断处理; 环形缓冲区(virtqueue等)
虚拟化: 共享内存(guest物理内存映射); 中断注入(irqfd);
        VM exit(MMIO/PIO写触发); eventfd; virtio队列三件套
        (descriptor table/available ring/used ring)
文件:  unix socket文件只是地址; 连接是fd到fd的内核缓冲管道

## 反面模式

- 只给类比不讲机制: "vsock就是虚拟串口"——然后呢？
- 停留在组件图: 画了盒子连线，没画数据怎么流
- 黑盒化: "内核自动处理了"——具体是哪个内核机制？
- 名词替换名词: "muxer做多路复用"——多路复用的实际实现是什么？
- 对方不懂时重复同一层解释: 换比喻、换措辞，就是不往底层走

## 检查标准

讲解合格的标准：对方能否凭描述在心里模拟出每一步的实际操作？

- 每个"传送"都说清了载体(哪个syscall/哪段内存/哪个队列)
- 每个"通知"都说清了机制(中断/eventfd/epoll/VM exit)
- 每张表、每张图都对应到可执行的代码路径
- 没有用类比代替机制的黑洞
