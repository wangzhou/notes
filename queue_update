
+-----+-----+-----+-----+-----+-----+
| sqe | sqe | sqe | sqe | ... | sqe |
+-----+-----+-----+-----+-----+-----+
  ^ ^
  | |
  | |

sq_head = 0
sq_tail = 0
atomic_ref = 0

问题：两个独立的线程，一个写入队列，一个读出队列。可以正常判空，判满。

软件设计：软件上的变量控制变量有 sq_head, sq_tail, atomic_ref。

sq_tail: 当前写入位置(还没有写入)
sq_head: 当前应该读的位置(还没有读出)
atomic_ref: 写入队列中的sqe个数, 用来判断空满

以上三个变量的约束条件:

- 初始均是0
- 在写入前判满, 如果满禁止写入
- 在写入后 atomic_ref + 1 
- 在读出后 atomic_ref - 1


